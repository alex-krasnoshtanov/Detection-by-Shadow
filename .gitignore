# --- Challenge data (redistribution not ours to grant) -----------------------
data/
*.zip

# --- Model weights and run outputs ------------------------------------------
# Checkpoints are large and reproducible; see docs/experiments.md for the
# exact commands that regenerate them.
*.pt
*.pth
runs/
outputs/
# scripts/package_release.py builds a ~93 MB archive here; it belongs on a
# release, not in the history.
release/

# --- Python -----------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/

# uv resolves torch differently per platform (CPU wheels on the CI runner,
# CUDA locally), so a committed lock would be wrong for half the readers.
# CI keys its cache on pyproject.toml instead.
uv.lock

# --- Tooling ----------------------------------------------------------------
.pytest_cache/
.ruff_cache/
.mypy_cache/
.ipynb_checkpoints/
.coverage
htmlcov/

# --- OS / editor ------------------------------------------------------------
.DS_Store
Thumbs.db
.vscode/
.idea/
